<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Parking_Mobile_App_Group3.phoneapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android3200StarterApplication">
        <activity android:name=".CheckInCheckOutActivity"></activity>
        <activity android:name=".ConfirmLotActivity" />
        <activity android:name=".CustomerFindLotActivity" />
        <activity android:name=".ManageLotsActivity" />
        <activity android:name=".LogInSignUpActivity" />
        <activity android:name=".ActivityWithUser" />
        <activity android:name=".AttendantMainActivity" />
        <activity android:name=".OwnerMainActivity" />
        <activity android:name=".CustomerMainActivity" />
        <activity android:name=".RegAttenActivity" />
        <activity android:name=".RegOwnerActivity" />
        <activity android:name=".RegCustomerActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>